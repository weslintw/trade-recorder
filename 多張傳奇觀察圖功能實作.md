# 多張傳奇觀察圖功能實作總結

## 功能需求
實現在「傳奇觀察圖」區域支援多張圖片上傳,當使用者貼上一張圖片後,右邊會自動出現新的空白上傳區域,讓使用者可以繼續貼上更多圖片。同時確保所有圖片都能在分享功能中正確顯示。

## 前端修改

### 1. LegendStrategy.svelte
**檔案路徑**: `frontend/src/components/trade-form/LegendStrategy.svelte`

**主要變更**:
- 將單張圖片 (`entry_strategy_image`) 改為支援多張圖片的陣列結構 (`legend_images`)
- 新增 `imageSlots` 響應式變數,自動在已有圖片後添加一個空白上傳區域
- 更新 `handleStrategyImagePaste` 函數,支援按索引上傳圖片
- 更新 `removeStrategyImage` 函數,支援按索引刪除圖片
- 添加網格佈局 CSS (`legend-images-grid`),讓多張圖片並排顯示

**資料結構**:
```javascript
formData.legend_images = [
  { image: "path1", originalImage: "path1" },
  { image: "path2", originalImage: "path2" },
  ...
]
```

### 2. SharedTradeDetail.svelte
**檔案路徑**: `frontend/src/components/SharedTradeDetail.svelte`

**主要變更**:
- 添加對新 `legend_images` 陣列格式的支援
- 保留對舊版 `entry_strategy_image` 單張圖片格式的向下相容性
- 使用 `#each` 迴圈顯示多張傳奇觀察圖
- 每張圖片都可以點擊放大查看

## 後端修改

### 1. 資料模型 (models/trade.go)
**檔案路徑**: `backend/internal/models/trade.go`

**主要變更**:
- 在 `Trade` 結構中添加 `LegendImages *string` 欄位 (JSON 陣列格式)
- 在 `TradeCreate` 結構中添加 `LegendImages string` 欄位

### 2. 資料庫遷移 (database/db.go)
**檔案路徑**: `backend/internal/database/db.go`

**主要變更**:
- 添加資料庫遷移語句: `db.Exec("ALTER TABLE trades ADD COLUMN legend_images TEXT;")`
- 此欄位儲存 JSON 陣列格式的圖片資料

### 3. 交易處理器 (handlers/trade.go)
**檔案路徑**: `backend/internal/handlers/trade.go`

**主要變更**:
- **GetTrade**: 在 SELECT 和 Scan 中添加 `legend_images` 欄位
- **CreateTrade**: 在 INSERT 語句中添加 `legend_images` 欄位
- **UpdateTrade**: 在 UPDATE 語句中添加 `legend_images` 欄位

### 4. 分享處理器 (handlers/share.go)
**檔案路徑**: `backend/internal/handlers/share.go`

**主要變更**:
- 在 `GetTradeInternal` 函數的 SELECT 和 Scan 中添加 `legend_images` 欄位
- 確保分享的交易紀錄包含所有傳奇觀察圖

## 向下相容性

### 前端相容性
`SharedTradeDetail.svelte` 使用條件判斷來支援兩種格式:
```svelte
{#if trade.legend_images && Array.isArray(trade.legend_images) && trade.legend_images.length > 0}
  <!-- 顯示新格式的多張圖片 -->
{:else if trade.entry_strategy_image}
  <!-- 顯示舊格式的單張圖片 -->
{/if}
```

### 後端相容性
- 資料庫使用 `ALTER TABLE ADD COLUMN` 語句,不會影響現有資料
- 新欄位 `legend_images` 允許 NULL 值,舊紀錄保持不變
- 舊紀錄仍可使用 `entry_strategy_image` 欄位

## 測試建議

1. **新建交易紀錄**:
   - 測試貼上第一張圖片後,是否自動出現第二個空白區域
   - 測試貼上多張圖片,確認都能正確顯示
   - 測試刪除圖片功能

2. **分享功能**:
   - 測試分享包含多張傳奇觀察圖的交易紀錄
   - 確認分享頁面能正確顯示所有圖片
   - 測試圖片點擊放大功能

3. **向下相容性**:
   - 測試舊有交易紀錄(使用 `entry_strategy_image`)是否仍能正常顯示
   - 測試編輯舊紀錄並添加新圖片

4. **資料庫遷移**:
   - 確認後端啟動時資料庫遷移成功執行
   - 檢查 `legend_images` 欄位是否正確添加到 `trades` 表

## 部署注意事項

1. 後端需要重新啟動以執行資料庫遷移
2. 前端需要重新編譯並部署
3. 建議先在測試環境驗證功能正常後再部署到生產環境
